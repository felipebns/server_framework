## Como que frameworks fazem para "subir" uma página localhost ? 

### Esse projeto busca entender como esse processo acontece por trás dos panos e permite o usuário subir um servidor simples com html, css e js estático + um backend simples com banco de dados local

### Preparando ambiente go

```
go mod init server_framework
```